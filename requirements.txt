# runtime dependencies. Docker image에 들어가는 것은 이 파일뿐이다.
# 개발/테스트용은 requirements-dev.txt.
fastapi==0.121.2
uvicorn==0.40.0
# Apple identity token(JWT/JWK) 검증. RSA/ASN.1/JWT parsing을 직접 만들지 않는다.
# pyjwt[crypto] = PyJWT + cryptography(RSA signature 검증).
pyjwt[crypto]==2.12.0
# Firestore. iOS는 Firestore에 직접 붙지 않는다 — 이 backend만 접근한다.
google-cloud-firestore==2.22.0
# AI 생성 결과를 잠시 두는 private bucket. **client는 bucket에 직접 붙지 않는다** —
# signed URL을 만들지 않고 우리 endpoint로만 스트리밍한다.
google-cloud-storage==3.4.1
# Apple StoreKit transaction JWS 검증. **x5c 인증서 체인 검증기를 직접 만들지 않는다** —
# 체인·만료·폐기 판단을 우리가 지면 틀렸을 때 조각이 공짜가 된다.
# App Store Server **API**는 부르지 않으므로 .p8 / issuerId / keyId가 필요 없다.
app-store-server-library==3.1.2
